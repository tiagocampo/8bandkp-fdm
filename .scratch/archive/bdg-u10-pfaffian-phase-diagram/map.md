# Wayfinder U10 Map — Pfaffian phase-diagram close-out

> Map file (wayfinder:map). Lives at .scratch/bdg-u10-pfaffian-phase-diagram/map.md per repo convention (local-markdown tracker, no setup-matt-pocock-skills run).

**Charting session ended 2026-07-26.** Decisions logged: minimal scope (Q1), span Lutchyn-Oreg window (Q2), drop z2 remap (Q3), per-B min-|Pf| proxy (Q4), shell regression test (Q5), 6-ticket flow (Q6), zero research tickets (Q7). Frontier tickets T1, T2, T3 open and unblocked.

**T1a closed 2026-07-26** (commit `1ee9ba2`, branch `feat/bdg-u10-pfaffian-phase-diagram`): seam extended with optional `best_pf_abs` out-arg through `wire_pfaffian_witness_sweep` and `eval_bdg_pfaffian_witness_csr`. Frontier re-shaped: T1b (now unblocked), T2, T3 still takeable.

**Post-merge state (2026-07-26):** PR #42 MERGED `main@257b3c4` 2026-07-26T17:34Z (squash), with branch-local fixes `6e37bb9..e368be1` folded into the squash commit. The seam SSOT, slim Pfaffian plug-in, heuristic retirement, and 3-witness gate that U10 was waiting on are now on `main`; the 3-witness gate precondition (`gate_row_colormap_present` reading `z2 == -1` at `mu ≈ 0.6601`) is the active acceptance surface, and the 4-witness slim-Pfaffian row's destination is deferred to U13 (per ticket 05 of `.scratch/archive/bdg-evaluator-pfaffian/`). Map moved from chart-in-progress to work-mode — T1, T2, T3 are takeable.

## Destination

Close U10 end-to-end with the `wire_bdg` sweep driving the slim projected Pfaffian witness (per `eval_bdg_pfaffian_witness_csr` seam sibling) and producing a regenerated, **non-flat (B, μ) phase diagram** whose `z2` column is written in the Pfaffian-native convention (`−1/+1/0` ∈ {topological, trivial, closure}). The acceptance gate precondition (`gate_row_colormap_present` reading a `z2 == −1` row at `mu ≈ μ_c`) returns True; a per-B `min-|Pf|` proxy is emitted so the 4th gate witness (`bcrit_pfaffian`) is approximate-not-deferred. A new shell regression test wires the non-flat assertion into ctest.

## Notes

- **Skills to consult**: `mattpocock-skills:wayfinder` (this map), `mattpocock-skills:writing-skills`, `mattpocock-skills:test-driven-development` (TDD discipline); **`mattpocock-skills:domain-modeling`** when scoping the slim Pfaffian contract vs. the full Bloch-Pfaffian (U13-deferred).
- **Codebase rules**: read `src/physics/AGENTS.md` + `tests/integration/AGENTS.md` + `scripts/AGENTS.md` before touching files; CLAUDE.md **gotchas** apply (pFUnit single-line `@assertEqual`, OMP thread cap, `cp -i` shell alias, `*.mod` shadowing).
- **Workflow preferences**: per `feedback_style.md` (design-first, no in-PR debounce on review fixes), per `feedback_no_coauthor_trailer`, per `feedback_pfunit_error_stop_uncatchable` (rejection-class tests in shell, not pFUnit), per `codebase-doc-drift-prevention` (every behavior PR updates spec + plan status footers + archives closed PRD).
- **Standing decisions** (per U2 close-out, see `.scratch/archive/bdg-evaluator-pfaffian/`):
  - `bdg_observables.f90` seam SSOT — `bdg_pfaffian_params_t` factory validation, `bdg_default_pfaffian_floor` SSOT.
  - `compute_z2_gap`/`compute_z2_gap_edge` → `*_bhz_heuristic` rename (per `project_bdg_z2_gap_helper_fate`).
  - `topology.sweep_model = bhz_analytic` UNTOUCHED — KTD8 invariant.
  - **z2 column convention (T3, 2026-07-27):** `wire_bdg` path emits Pfaffian-native `{-1,+1,0}` directly from `s2_sign`; non-wire_bdg paths (BHZ via `compute_z2_gap_sweep`, QW via `compute_qw_fukane_gap_sweep`) retain the heuristic `{0,1}` convention at the helper level. `write_z2_phase_diagram` annotates the per-file convention via a `# z2 semantics:` header line. Single convention enforced by header reading, not by writer-time conversion.
  - U13 periodic/Bloch BdG BLOCKING-EMPIRICAL, deferred per main plan §U13 and CLAUDE.md Known Issues.

## Decisions so far

<!-- decisions log: one line per closed ticket; bare slug in front of gist, link to full ticket body.
     Chart-session grillings (Q1–Q7) were resolved during the 2026-07-26 chart session and recorded
     compactly in HANDOFF.md line 5; they sit on the index here as decisions, not as separately-filed
     child tickets (per local-markdown tracker fallback). Per-ticket cross-references: HANDOFF.md →. -->

- [Q1 — minimal U10 scope (close thin slice, U11/U12/U13 out)](HANDOFF.md#destin) — accept "close U10 end-to-end with seam-witness + non-flat colormap + ≥2-value regression" as the destination; U9 / U11 / U12 / U13 are explicitly out (U13 BLOCKING-EMPIRICAL).
- [Q2 — μ-window spans Lutchyn-Oreg band ~0.5–1.0 eV](HANDOFF.md#destin) — single coarse sweep `gap_sweep_mu_{min,max}=[0.5, 1.0]`, `nMu=11` (11×11 B×μ grid per lecture 13 precedent); preserves `bcrit_curve ≈ 2.85 T`.
- [Q3 — drop inverted z2 remap; write Pfaffian-native {-1, +1, 0}](tickets/T3_native_z2.md) — the post-#42 `main_topology.f90:1367-1396` remap (`s2_sign ∈ {-1, +1, 0} → z2 ∈ {1, 0, heuristic}`) inverts Pfaffian-native semantics; remove it. BHZ-only `compute_z2_gap_sweep` retains `*_bhz_heuristic` (KTD8).
- [Q4 — per-B min-|Pf| proxy file via `wire_slim_pfaffian_witness.dat`](tickets/T1_min_pf_proxy.md) — emit a per-B open-chain projected `|Pf|_min` so the 4th gate witness `bcrit_pfaffian` is approximate-not-deferred; full Bloch-Pfaffian at PHS-invariant k stays U13.
- [Q5 — shell regression test, not pFUnit](tickets/T4_regression_test.md) — `test_wire_pfaffian_phase_diagram_noflat.sh` mirrors the existing `test_topology_sweep.sh` pattern (per `feedback_pfunit_error_stop_uncatchable`); asserts non-flat colormap + proxy file presence.
- [T2 — Widen μ-window to Lutchyn-Oreg span (with writer-schema change)](tickets/T2_mu_window.md) — grill resolved 2026-07-26 (11 questions, scope locked); execution 2026-07-27 (8-item scope, 51/51 unit + 5/5 wire-BdG regression green): config-driven writer paths under `[topology] phase_diagram_file` + `slim_pfaffian_witness_file` (default values preserve canonical fixture behavior), new sibling fixture `wire_inas_gaas_bdg_topological_phase.toml` overrides both keys + widens B-grid to nB=6 with tight mu-window [0.6600, 0.6602] eV (FEST physics window `±5·δ₀` around E=0 constrains μ-window to ±0.001 eV of conduction-band edge 0.6601 eV; wider μ sweeps leave Majorana-mode subspace empty and FEAST fails), new `regression_wire_bdg_topological_phase` ctest entry under `regression` label (900 s timeout), plan §U10 + §R9 amended, 2 new TDD parser @tests pass.
- [T4 — Non-flat colormap regression test](tickets/T4_regression_test.md) — closed BLOCKED 2026-07-27 (no source edit, no commit, no new ctest entry). User direction: "don't bug code that is breaking the physics." T4r research subagent verdict: BUG REFUTED (slim CSR Pfaffian correctly constructs local 4×4 omega at band-7,8 subblock indices; Ponytail rule satisfied; 51/51 unit + 6 BdG/Pfaffian tests pin correctness). 5 probe fixtures enumerated (probes 1–5): FEST envelope widest safe μ ∈ [0.657, 0.663] eV around edge 0.6601 eV (Δμ = 0.006 eV); 231/231 cells s2_sign=-1 inside envelope. Closure regime (`s2_sign=0`) requires full Bloch-Pfaffian + periodic Peierls-twist (U13, BLOCKING-EMPIRICAL). Non-flat colormap assertion deferred to U13. Pick-D bookkeeping (BACKLOG.md Phase 26 row + parent plan §U10 footer + REVIEW.md row 79) appended in commit `1a7767e`.
- [T5 — Gate precondition update z2==-1 native (acceptance surfaces)](tickets/T5_gate_precondition.md) — closed 2026-07-27 (full cleanup scope): 2 commits `1a7767e` (Pick-D bookkeeping) + `d17f75b` (source/test/doc-drift). Q4 approximation phrase "approximation (open-chain projected; full Bloch-Pfaffian deferred U13)" applied to lecture_13_topological.py lines 238-246, 255, 327; test_lecture_13_acceptance_gate.sh comment blocks 49-56, 79-86, 95-111 + deferred-branch label; test_slim_pfaffian_witness_projection.py lines 15, 43-44, 107-109, 167, 178-179 (preserved logic + deferred-else fallback per scope δ). Logic unchanged (gate already auto-detects numeric Pfaffian witness at line 81 → 4-witness when emitted, 3-witness fallback when absent/truncated). 51/51 unit + 4/4 wire_bdg regression + 2/2 slim_pfaffian regression green. BHZ unit-test churn (`test_edge_states.pf:80`, `test_z2_invariant.pf:74`, `test_phase_diagram.pf:178` pinning BHZ to `{0,1}`) deliberately untouched per T3 standing decision.
- [T6 — Per-B min-|Pf| extraction Lutchyn-Oreg criterion](tickets/T6_lutchyn_oreg_proxy.md) — closed 2026-07-27 (grilling resolution: try (c) → fall back to (a)). Empirical baseline (`wire_inas_gaas_bdg_topological_phase.toml` sibling, nB=6 B∈[0,5]T): all 6 per-B `|Pf|_min = 4.0E-08` (FEST precision floor, well above `bdg_default_pfaffian_floor` SSOT 1.0e-12_dp) → relative variance 0.0e+00, proxy numerically degenerate. Resolution (c) attempted (re-derive `bcrit_pfaffian` from gap data in `z2_phase_diagram.dat`) failed: would duplicate `bcrit_2d` (same file, same gap-min strategy at lecture_13_topological.py:178-196) — contradicts U10 destination line "4th gate witness approximate-not-deferred". Resolution (a) executed: new "file-present-but-all-at-floor" branch at lecture_13_topological.py falls back to approximation label (non-numeric, gate auto-detects → 3-witness fallback via `test_lecture_13_acceptance_gate.sh:86`); module-level `_PFAFFIAN_DEGENERACY_TOL=1e-6` (relative-variance threshold, dimensionless, survives any future SSOT update). Three failure-mode branches verified end-to-end (file absent / file saturated / file varying). Full Bloch-Pfaffian remains U13 destination. **Map frontier empty after this resolution.** Pick-B parallel track remains: PR #43 review of T1b+T2+T3.
- [T3 — Drop z2 remap inversion; write Pfaffian-native {-1, +1, 0}](tickets/T3_native_z2.md) — grill resolved 2026-07-27 (3 questions, scope locked): **wire_bdg path** emits Pfaffian-native `{-1,+1,0}` directly from `s2_sign` (no remap, no BHZ-heuristic fallback at closure); **BHZ-only `compute_z2_gap_sweep` path** retains `compute_z2_gap_bhz_heuristic` and `eval_bhz_analytic` `{0,1}` convention (preserves 7+ existing unit tests at `test_z2_invariant.pf:41,74,266,299,342,343`, `test_edge_states.pf:44,80`, `test_phase_diagram.pf:178`); **`write_z2_phase_diagram`** writes a `# z2 semantics:` header line documenting per-file convention (wire_bdg native / non-wire_bdg heuristic). One source edit in `main_topology.f90:1420-1434` (collapsed remap+fallback into `z2 = s2_sign`); header annotation in `outputFunctions.f90:780-789`. TDD-red pinned in `test_wire_bdg_topological_phase.sh` (asserts z2 column subset of `{-1,0,1}` AND at least one row reads -1). Resolves "BHZ-only dispatch return-path contract" fog bullet. Stands as a new standing decision: **wire_bdg path uses Pfaffian-native {-1,+1,0}; non-wire_bdg paths use heuristic {0,1}; write_z2_phase_diagram header annotates which**.
- [Q6 — 6-ticket flow (T1–T6)](tickets/) — T1/T5 = task (AFK), T2/T3/T6 = grilling (HITL); T1+T2+T3 unblock T4+T5+T6. No research tickets.
- [Q7 — zero research tickets](HANDOFF.md#files-to-read-first) — all required facts in-repo (Lutchyn-Oreg criterion, slim Pfaffian contract, BHZ heuristic scope); no `/research` subagent dispatches needed.
- [T1 — Wire min-|Pf| per-B proxy producer (informational, no schema break)](tickets/T1_min_pf_proxy.md) — resolved 2026-07-26: **NO** — `best_pf` is already computed inside `wire_pfaffian_witness_sweep` (topological_analysis.f90:1685-1716) but only `s2_sign` crosses the seam boundary. Splits into T1a (seam return) + T1b (per-B producer); doc-drift on `tests/integration/test_slim_pfaffian_witness_projection.py:15` "RESERVED for U13" reservation flagged separately.
- [T2 — Widen μ-window to Lutchyn-Oreg span (with writer-schema change)](tickets/T2_mu_window.md) — **grill resolved 2026-07-26** (11 questions, scope locked): new sibling fixture `wire_inas_gaas_bdg_topological_phase.toml` (μ ∈ [0.5, 1.0] eV, nMu=11, B ∈ [0, 5] T, nB=11) PLUS config-driven writer file paths (`[topology] phase_diagram_file` + `slim_pfaffian_witness_file`, default values preserve canonical behavior; both `write_z2_phase_diagram` and `write_wire_slim_pfaffian_witness` get the schema change). 8-item implementation scope in ticket body: new fixture, defs.f90 fields, parser update, writer concat, parser unit test, CMakeLists entry, plan amend. `lecture_13_topological.py` path lookups deferred to U11. **Execution ticket** (task, AFK) — next session claims and ships.
- [T1a — Pfaffian magnitude return channel through the seam](tickets/T1a_pfaffian_magnitude_seam.md) — closed 2026-07-26 commit `1ee9ba2`. `wire_pfaffian_witness_sweep` now exposes the per-(B, mu) max-site `|Pf|` magnitude via a new out-arg; seam sibling `eval_bdg_pfaffian_witness_csr` exposes the same as an OPTIONAL out-arg (preserves existing 3-arg call form for `eval_wire_bdg_gap`). 51/51 unit + 6 BdG/Pfaffian tests green. **Drift from ticket text**: only ONE production call site exists post-#42 (the ticket's "three call sites" claim was stale; U2 close-out retired `run_bdg_wire`/`run_bdg_qw` from the seam path). Doc-drift on `bdg_observables.f90:6-13` "all three per-point call sites" comment filed in Not-yet-specified.
- [T1b — Per-B min-|Pf| proxy producer](tickets/T1b_per_b_min_pf_proxy.md) — closed 2026-07-26 on branch `feat/bdg-u10-pfaffian-phase-diagram`. `eval_wire_bdg_gap` + `compute_wire_bdg_gap_sweep` thread the seam's `best_pf_abs` out-arg through to a new `(nMu, nB)` magnitude grid; `run_gap_sweep` folds it into a per-B min over the mu-window and calls the new `write_wire_slim_pfaffian_witness` writer (outputFunctions.f90). Producer emits `output/wire_slim_pfaffian_witness.dat` in literal-token `B=<val> <space> |Pf|=<val>` format matching the lecture 13 acceptance-gate reader's regex (Fortran's `ES16.8` width padding trimmed via `trim(adjustl(...))`). New smoke test `regression_wire_slim_pfaffian_witness` registered in `tests/CMakeLists.txt`. 51/51 unit + 3 wire-BdG regression tests green. **Drift from ticket text**: ticket acceptance said format `B(T) |Pf|_min` but lecture 13 regex requires `B=val |Pf|=val` literal tokens — caught and fixed at execution time.

## Tickets (open frontier)

- ~~T1 — Wire min-|Pf| per-B proxy producer (informational, no schema break).~~ **closed 2026-07-26** as "no transparent path". Splits into T1a + T1b.
- ~~T1a — [Pfaffian magnitude return channel through the seam](tickets/T1a_pfaffian_magnitude_seam.md).~~ **closed 2026-07-26** commit `1ee9ba2`.
- ~~T1b — [Per-B min-|Pf| proxy producer](tickets/T1b_per_b_min_pf_proxy.md).~~ **closed 2026-07-26** on branch `feat/bdg-u10-pfaffian-phase-diagram` (uncommitted at session close).
- T2 — [Widen μ-window to Lutchyn-Oreg span](tickets/T2_mu_window.md) — grill, HITL. (no blocker).
- ~~T3 — [Drop z2 remap inversion; write Pfaffian-native](tickets/T3_native_z2.md).~~ **closed 2026-07-27** commit `29e85c5` on branch `feat/bdg-u10-pfaffian-phase-diagram`. Wire path emits Pfaffian-native `{-1,+1,0}` directly; BHZ-only path retains heuristic `{0,1}`; header annotation per-file. 51/51 unit + 5/5 wire-BdG regression green.
- ~~T4 — [Non-flat colormap regression test](tickets/T4_regression_test.md).~~ **closed BLOCKED 2026-07-27** — non-flat colormap assertion is physically unsatisfiable inside the slim projected Pfaffian's signal regime (probes 1–5: 231/231 cells `s2_sign=-1` across all probed (B, μ, Bx) configurations inside the FEST envelope). Manufacturing `s2_sign=0` (closure) or forcing a flip to `+1` would bug working physics per user direction 2026-07-27 ("don't bug code that is breaking the physics"). Non-flat colormap assertion deferred to **U13** (full Bloch-Pfaffian + periodic Peierls-twist, BLOCKING-EMPIRICAL). **Action taken (none on source)**: probe fixtures deleted, `input.toml` restored, this ticket closed. **Pointer**: BACKLOG.md Phase 26 row + parent plan §U10 status footer + REVIEW.md row 79 must append "T4 closed BLOCKED → deferred to U13". T5 unaffected (gate reads `z2 == -1` natively per T3, no flat-colormap assumption in T5's logic).
- **T4r — [Research: latent Pfaffian S2 projection bug?](tickets/T4r_pfaffian_research.md)** — research, AFK. **RESOLVED 2026-07-27**: BUG REFUTED. Research subagent traced `eval_bdg_pfaffian_witness_csr` → `wire_pfaffian_witness_sweep` and confirmed the projection correctly constructs a local 4×4 omega at band-7,8 subblock indices (not sliced from a larger 16×16), satisfying the U2 Ponytail rule. Five evidence bullets (local-omega construction, subblock row/col selection, CSR gather exactness, synthetic-fixture RED-GREEN pin, seam-threading pin via looser-floor test) — full report in ticket body. **Conclusion**: the slim CSR Pfaffian is correctly implemented. The all-`-1` colormap inside the FEST envelope is the genuine physical answer, not a structural artifact. **No fix required; no source change.** T4 BLOCKED closure stands; U13 (full Bloch-Pfaffian + periodic Peierls) remains the destination for any non-flat z2 colormap assertion.
- ~~T5 — [Gate precondition update z2==-1 native](tickets/T5_gate_precondition.md).~~ **closed 2026-07-27** (full cleanup, 2 commits `1a7767e` + `d17f75b`); 51/51 unit + 4/4 wire_bdg regression + 2/2 slim_pfaffian regression green.
- ~~T6 — [Per-B min-|Pf| extraction Lutchyn-Oreg criterion](tickets/T6_lutchyn_oreg_proxy.md).~~ **closed 2026-07-27** (per-B `|Pf|_min` saturation detection, grilling resolution (c)→(a)); new "file-present-but-all-at-floor" branch at `lecture_13_topological.py` + module-level `_PFAFFIAN_DEGENERACY_TOL=1e-6`; full report in ticket body §Resolution. 51/51 unit + 3/3 wire_bdg regression + 2/2 slim_pfaffian regression + 1/1 lecture 13 acceptance gate green.

**No research tickets** (per grilling Q7): all required facts are in-repo (Lutchyn-Oreg criterion not needed for the proxy gate; full Bloch-Pfaffian deferred to U13).

Edge map (visual):
```
  T1a ──→ T1b ──┬────→ T4 (BLOCKED → U13) ── (was: → T6, now: T6 unblocked directly via T1b)
                ├────→ T5
  T2 ───────────┘
  T3 ───────────┴────────────────────────────→ T5
                            └→ T4r (RESOLVED 2026-07-27 — bug REFUTED; T4 BLOCKED stands)
```
Frontier (open, unblocked, unclaimed): **empty** (T6 closed 2026-07-27 — per-B |Pf|_min saturation detection at lecture_13_topological.py). **All U10 in-flight tickets closed**; remaining open work lives outside this map (PR #43 review of T1b+T2+T3 parallel track; U9 spectral+LDOS separate unit; U11 lecture revamp separate unit; U13 full Bloch-Pfaffian BLOCKING-EMPIRICAL deferred).

## Post-U10 PR review findings (2026-08-08)

PR review pass (4 parallel reviewers: code-reviewer, silent-failure-hunter, comment-analyzer, pr-test-analyzer) surfaced 12 issues across 8 commits. Branch is merge-ready with these tracked as follow-up tickets:

- **[P1 — `test_slim_pfaffian_witness_projection.py` parser format mismatch (CRITICAL)](tickets/P1_slim_pf_parser_format.md)** — parser looks for `slim_pf_sign=` rows but T1b emitter produces `B=<val> |Pf|=<val>` rows. Confirmed FAIL on direct invocation. Not in ctest net (silent).
- **[P2 — `pmax > 0` short-circuit (CRITICAL)](tickets/P2_pmax_zero_short_circuit.md)** — `lecture_13_topological.py:245` skips the degeneracy branch when witness is all-zero (closure-only regime), falls through to `min(pf_mags, key=lambda B: pf_mags[B])` which returns first B.
- **[P3 — T6 saturation synthetic-fixture test (IMPORTANT)](tickets/P3_t6_synthetic_fixture_test.md)** — T6's "three failure modes verified" is manual, not automated. Blocked by P2 (adds 4th case).
- **[P4 — 3 stale line-number citations in test comments (IMPORTANT)](tickets/P4_stale_line_citations.md)** — line numbers shifted by T1b seam plumbing / T6 lecture script changes.
- **[P5 — Stale docstrings in `lecture_13_topological.py` (T5 scope gap) (IMPORTANT)](tickets/P5_stale_lecture_docstrings.md)** — T5 "full cleanup" missed 3 docstrings.
- **[P6 — Pick-D scope gaps (T1a/T1b markers + BACKLOG row 26 contradiction) (IMPORTANT)](tickets/P6_pick_d_scope_gaps.md)** — 2 status-doc items missed.
- **[P7 — TOML validation for new writer-path keys (IMPORTANT)](tickets/P7_writer_path_validation.md)** — empty / whitespace / absolute path / path traversal / >64 chars not validated.
- **[P8 — Defense-in-depth polish batch (5 SUGGESTIONS)](tickets/P8_polish_batch.md)** — `# z2 semantics:` header pin, `intent(out)` initialization, unused `HERE`, trailing newlines, bash regex tightening, YAGNI on optional arg.

**Recommended merge decision:** ship PR #43 as-is (branch +8 commits, all tests green, 2 CRITICALs + 5 IMPORTANTS filed as follow-up tickets). C1 and C2 are real bugs that will surface in ad-hoc test invocation; not fixing them in this PR is acceptable because the test net (`ctest -l regression`) doesn't currently exercise the failing path, but P1 must land before the next release tag.

## Not yet specified

<!-- fog: questions that can't be phrased sharply yet; clears via the frontier -->

- **BHZ-only `bhz_analytic` dispatch return-path contract** when `s2_sign == 0` no longer writes `z2=1` from BHZ heuristic (Q3 accepted, no more remap). What does `compute_z2_gap_sweep`'s BHZ path write? Spec retains heuristic for `bhz_analytic`, but its column-write semantics may now diverge from wire path — investigate during execution, may need a separate `wire_bdz` z2-internal-name or a column comment.
- ~~**Doc-drift ticket: `tests/integration/test_slim_pfaffian_witness_projection.py:15` "RESERVED for U13" reservation is now stale**~~ — STALE fog bullet (already aligned post-T1b commit `0a62143`; doc now correctly describes U10 emission). No fix needed.
- **Doc-drift ticket: `src/physics/bdg_observables.f90` scope comment** — fixed in commit `cd1d427` (2026-08-08). Original lines 6-13 named "all three per-point call sites" but post-#42 only `eval_wire_bdg_gap` consumes the Pfaffian seam sibling; per-point `eval_bdg_point` has four call sites (`run_bdg_wire` ×2, `run_bdg_qw`, `eval_wire_bdg_gap`). Restructured the header to distinguish the two seams and their consumer counts.

## Out of scope

<!-- pre-conditions met: U2 seam siblings + slim Pfaffian plug-in shipped via PR #42 merge
     (main@257b3c4, 2026-07-26T17:34Z). U2 closed. -->

- U9 (BdG spectral function + LDOS) — separate unit, different code path.
- U11 (lecture 13 revamp + bcrit consistency table) — depends on U10's regenerated values, but the lecture stamping is its own ticket.
- U12 (acceptance gate formalization + coverage) — depends on U11.
- U13 (periodic/Bloch BdG + strict PHS-invariant Pfaffian) — BLOCKING-EMPIRICAL, deferred per main plan §U13 + CLAUDE.md Known Issues.
- QW Fu-Kane parity migration to Pfaffian — different invariant (class-AII QSHE ≠ class-D BdG), explicitly out of scope per spec §KTD4 + per Q1 acceptance.
- Majorana polarization (Sticlet P_M) — emitter is BLOCKING-EMPIRICAL on canonical wire config (verify_majorana_polarization.py:13-23), separate ticket.

