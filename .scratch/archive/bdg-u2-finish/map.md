---
title: BdG U2 finish — wire SSOT + tighten tests + retire orphans + amend spec + scratch consolidation
type: wayfinder:map
status: complete
date: 2026-07-13
owner: Tiago de Campos
branch: feat/bdg-u2-actual-ship
closes: PR #42 review findings (9 Standards + 3 Spec) + Codacy 2 high ErrorProne flags
supersedes: nothing (extends PR #42, not a re-do)
related: .scratch/archive/bdg-evaluator-pfaffian/spec.md; .scratch/archive/bdg-evaluator-pfaffian/map.md; design.md (in this dir; the as-built retrospective)
---

# BdG U2 finish — wayfinder map

**Status:** complete (2026-07-25). Eight tickets resolved. When this map merges, auto-archives alongside `.scratch/bdg-u2-actual-ship/` → `.scratch/archive/bdg-u2-finish/` per the consolidation decision in `design.md` §"Cross-References".

## Destination

Land a follow-up PR on `feat/bdg-u2-actual-ship` that closes every PR-#42 review finding flagged by the two-axis code-review (Standards + Spec sub-agents) plus the 2 Codacy high-severity `ErrorProne` flags, then amends `User Story 5` in `.scratch/archive/bdg-evaluator-pfaffian/spec.md` to encode the discovered invariant (S1+S2 strict sign agreement is the Majorana-basis Pfaffian problem = Issue 05 / U13, not a U2 delta; U2 accepts `s2 ∈ {-1, 0, +1}` with `s2 /= 0` on non-diagonal synthetic fixtures as the GREEN contract). PR is single-FSM-extension of PR #42 (no squash, multi-commit TDD).

## Notes

- **Source-of-truth design:** `design.md` (213 lines, as-built retrospective + 14-step solution). All decisions in this map trace to a numbered item there. Where design.md frontmatter and this map disagree, **design.md wins** (it carries the file:line citations).
- **Standing locked-decisions** (do NOT re-litigate — these were locked by the previous map at `.scratch/archive/bdg-evaluator-pfaffian/`):
  - `eval_bdg_pfaffian_witness_csr` / `eval_bdg_kitaev_majorana` are the seam siblings in `bdg_observables.f90` (ticket 01 answer).
  - `bdg_default_pfaffian_floor = 1.0e-12_dp` SSOT lives at `bdg_observables.f90:45`; **promise**: this map retires the 3 hard-coded `1.0e-12_dp` literals at `topological_analysis.f90:1680, :1698, :1781` — split between ticket 01 (`:1781` wire-up, `wire_pfaffian_witness_sweep` accepts `pfaffian_floor` arg) and ticket 02 (`:1680` and `:1698` retire with the deletion of dense `wire_pfaffian_witness`). The wire-up is on `wire_pfaffian_witness_sweep`, not "declare and forget".
  - `compute_z2_gap*` already renamed to `_bhz_heuristic` in commit `9c15a1e`; gap-closure fallback is `main_topology.f90:1380`.
  - 4-witness acceptance gate (1.0 T) already reads `output/z2_phase_diagram.dat` z2 column at μ ≈ 0.6601 (L13 lecture script). Not touched by this map.
- **Domain glossary:** `CONTEXT.md`, `UBIQUITOUS_LANGUAGE.md`. BdG Nambu layout: `src/physics/AGENTS.md` §"BdG Nambu structure".
- **ADRs:** 0001 (no polymorphic builder types); 0002 (no new TOML fields); 0007 (canonical hole block `-conjg(H₀(-k))`); 0008 (BdG P1 invariants, including `bdg_default_near_zero_frac = 0.001_dp`).
- **Engineering principles:** repo root `CLAUDE.md` → DRY (SSOT), YAGNI (orphan retirement > speculative generalization), KISS (params-type split is cleaner than a cross-domain field), SRP (`bdg_pfaffian_params_t` thin Pfaffian-domain type vs. evaluator-domain `bdg_eval_params_t`).
- **Skills every session should consult:**
  - `/superpowers:test-driven-development` — each code-item commits TDD-doubled.
  - `/superpowers:receiving-code-review` — when reviewing the Codacy output.
  - `/superpowers:verification-before-completion` — before claiming a ticket resolved: re-run the gate (ctest + ctest -L unit + ctest -R lecture_13_acceptance_gate + manual diff scan), record evidence, only then close.
- **Convention reminders:** pFUnit `@assertEqual`/`@assertTrue` are single-line (no `&` continuation per `feedback_pfunit_macro_single_line`); `error stop '<descriptive message>'` not `stop 1`; no Co-Authored-By trailer on commits (per `feedback_no_coauthor_trailer`).
- **Doc-drift prevention:** this PR touches both code AND doc surfaces; the 5 doc-propagation sites in ticket 07 carry verbatim wording so reviewers can grep for it (per `codebase-doc-drift-prevention.md` + `codebase-doc-drift-event-3.md`).

## Decisions so far

<!-- index — one line per closed ticket; the detail lives in the linked ticket body. -->

- [01 — SSOT wire-up: `bdg_pfaffian_params_t` from default to call site](issues/01-ssot-wire-up.md) — locked 2026-07-14, **executed 2026-07-25** (wayfinder work-through). Code landed in working tree on `feat/bdg-u2-actual-ship` (uncommitted): thin type `bdg_pfaffian_params_t` with default initial value `pfaffian_floor = bdg_default_pfaffian_floor`; factory `bdg_pfaffian_params_with_floor` carries the one `error stop`; `pfaffian_floor` field dropped from `bdg_eval_params_t` (SRP); seam sibling `eval_bdg_pfaffian_witness_csr` retyped to take `bdg_pfaffian_params_t` and threads `params%pfaffian_floor` into `wire_pfaffian_witness_sweep`; that helper gained a `pfaffian_floor` arg consumed at the former `:1781` literal; `main_topology.f90:1375` call site passes `bdg_pfaffian_params_t(pfaffian_floor = bdg_default_pfaffian_floor)`. Seam siblings stay non-`pure` (callee chain `complex_pfaffian`/`kitaev_majorana_number` in `src/math/pfaffian.f90` is not pure, ticket 01 doesn't own that module); wire-up only parameterizes `topological_analysis.f90:1781` — the 2 literals at `:1680`/`:1698` retire with `wire_pfaffian_witness` in ticket 02. **Verified:** `cmake --build build` clean; `ctest -L unit` 52/52 PASS; `grep 1.0e-12_dp` on topological_analysis.f90 returns exactly 2 (the dense-subroutine literals ticket 02 will delete). Edit footprint noted: the prior `test_bdg_eval_params_factory_uses_pfaffian_floor` test was deleted (it asserted the dropped field); its happy-path coverage is reasserted on the correct type by `test_bdg_pfaffian_params_default_uses_ssot` — ticket 03 should confirm/re-route if it wants the error-stop branch additionally pinned.
- [02 — Orphan retirement: delete dense `wire_pfaffian_witness`](issues/02-orphan-retirement.md) — locked 2026-07-13: DELETE dense `wire_pfaffian_witness` (1641-1707), `s1_project` (1809-1864), `s2_project` (1875-1921) — original ticket sub-decision 2 was empirically false (sweep performs inline CSR extraction, does NOT call either helper), corrected to delete both. DELETE `tests/unit/test_wire_pfaffian_witness.pf` entirely — original ticket sub-decision 3 premise ("0 hits on dense in test file") was empirically false (4 hits: L21 import + L115/L144/L172 calls), option (A) DELETE chosen over (B) MIGRATE since CSR-seam coverage already exists in `tests/unit/test_bdg_evaluator.pf`. DROP orphan `public :: wire_pfaffian_witness` at `topological_analysis.f90:40` + stale refs at `src/physics/AGENTS.md:39` and `tests/CMakeLists.txt:334-341` (all 3 net-new touch points the original ticket omitted). NO private stub. NO dense wrapper sibling. YAGNI. Result: `ctest -L unit` 49/49 PASS (52 baseline − 3 dense tests; +1 from ticket 01 happy-path placeholder); `topological_analysis.f90` drops ~280 lines; all 3 hard-coded `1.0e-12_dp` literals vanish from the file post-deletion.
- [04 — Codacy triage: 2 high `ErrorProne` flags](issues/04-codacy-triage.md) — locked 2026-07-13: BOTH flags are pyflakes F541 ("f-string is missing placeholders"), not Fortran `pure`-placement / allocatable-finalizer as design.md §"Error Handling" §"Codacy ErrorProne triage (inline)" anticipated. Source of truth: PR #42 Codacy check-run `86896052574` annotations endpoint (`gh api repos/tiagocampo/8bandkp-fdm/check-runs/86896052574/annotations`) — loadable without auth, unlike the Codacy app's PR-issues URL. Flag 1: `scripts/lecture_13_topological.py:396` — `print(f"FAIL: 4-witness disagreement exceeds tolerance")`. Flag 2: `tests/integration/verify_majorana_polarization.py:166` — `print(f"      Re-run wire BdG sweep to regenerate the colormap + polarization file.")`. Fix is 1-character per line (drop the `f` prefix), single commit per ticket sub-decision 2; apply-fix commit deferred to the next session that picks up the verification-gate runner (ticket 05). Predicted-patterns sub-decisions 3 (`pure` on seam siblings) and 4 (`error stop`/`stop 1`) are moot — no Codacy flags in either category. Unblocks tickets 05 and 07 (both require Codacy-clean per their blocking edges).
- [03 — Test contracts: SSOT error-stops + s2_sign tightening + meta-test](issues/03-test-contracts.md) — locked 2026-07-13: 7 sub-decisions pinned — (1) 2 error-stop tests on `bdg_pfaffian_params_with_floor` factory (zero + negative `pfaffian_floor`); (2) re-route 2 SSOT-pin tests through `bdg_pfaffian_params_t` factory (drop `bdg_eval_params_t%pfaffian_floor` field per ticket 01, add custom-floor flow-through assertion); (3) tighten `_nondiagonal_nonzero_sign` from `@assertTrue(s2_sign /= 0)` → `@assertEqual(-1, s2_sign)` on canonical fixture (preserves loosened-test rationale via meta-test in (5)); (4) new `test_seam_sibling_consumes_pfaffian_floor` = H1 receipt test that would have caught PR #42's declared-but-not-consumed SSOT; (5) meta-test `test_pfaffian_witness_spec_user_story_5_contract` pinning User Story 5 invariants (range + non-zeroness + specific sign) — name grep-able from future audits; (6) "Pf_real = -0.0225" analysis block lives at top-of-file in `test_bdg_pfaffian_witness_csr.pf` only (dedup is trivial after ticket 02 deletes `test_wire_pfaffian_witness.pf`); (7) EOF newline on `test_bdg_evaluator.pf` (H2). All pFUnit `@assertEqual`/`@assertTrue` are single-line per `feedback_pfunit_macro_single_line`. Out-of-scope per ticket body: 2 Codacy `ErrorProne` flags (→ 04, closed), doc propagation of meta-test name (→ 07), unit-count reconciliation (→ 05). **EXECUTED 2026-07-25** (wayfinder work-through): SD-2 found already done by ticket 01 (verified, not re-touched); **SD-1 dropped as INFEASIBLE** — pFUnit 4.16 cannot catch `error stop` (`tests/unit/test_parameters.pf:223` invariant; rejection tests live in `tests/integration/*.sh` exit-code checks, but the factory is not TOML/config-reachable today so a shell rejection test would need a superfluous driver + YAGNI knob). A literal `@test` calling the factory with `pfaffian_floor <= 0` would `error stop` the whole `ctest` process → GREEN-to-RED, a defect not a test. Error-stop contract stays enforced by factory source `bdg_observables.f90:153-154`. HITL decision recorded in `issues/03-test-contracts.md` "Execution" section. SD-3/4/5/6/7 landed in `test_bdg_pfaffian_witness_csr.pf` + `test_bdg_evaluator.pf`. Verified: `cmake --build build` clean; `ctest -L unit` 51/51 PASS (CSR target now carries 6 `@test` subroutines: the original 4 + `test_seam_sibling_consumes_pfaffian_floor` + `test_pfaffian_witness_spec_user_story_5_contract`; ctest target count unchanged at 51 — the targets-vs-subroutines gotcha).

## Not yet specified

- **Codacy flag details.** RESOLVED (ticket 04, 2026-07-13): the 2 high `ErrorProne` flags are pyflakes F541 (f-string missing placeholders), not Fortran issues — design.md's predicted patterns (`pure` placement, allocatable finalizer) were a false alarm. Source of truth: Codacy check-run annotations endpoint (no auth). Files: `scripts/lecture_13_topological.py:396` + `tests/integration/verify_majorana_polarization.py:166`. Fix: drop `f` prefix on both lines. Apply-fix commit handed off to next session.
- **Unit-test count drift.** `ctest-phase3-final.log` shows **52/52** PASS — the actual `ctest -L unit` total today is 52. Design.md §"Testing" treats 52 as canonical, but item 9 re-runs `ctest -L unit` AFTER all code/test changes land and locks the running total as the source-of-truth. With ticket 01's wiring contract, the running total after ticket 01's code lands = **53** (52 + 1 happy-path placeholder now compiling). Ticket 03 may add more.

## Out of scope

- **U9** (BdG spectral + LDOS), **U10** (bulk minigap + (B, μ) phase diagram), **U11** (lecture 13 full revamp), **U13** (periodic/Bloch BdG construction) — all carried forward per `design.md` §"Out of Scope".
- **Issue 05** (Majorana-basis Pfaffian projection yielding strict S1==S2). Separate scope; deferred. User Story 5 amendment in this map encodes the carve-out explicitly.
- **topological_analysis.f90 module split** (~1740 lines). Separate concern; BACKLOG Phase 18 architecture-deepening item.
- **`pure` attribute on `src/math/pfaffian.f90` helpers.** Ticket 01 sub-decision 3 defers this (different module, out of this PR's scope).
- **Codacy beyond the 2 high flags.** This map triages the 2 high flags; chasing unrelated style nits is not in scope.
- **PR review-process changes.** The PR fixes findings; it does not change how reviews happen.

## Tickets

| # | Title | Type | Blocked by | Status |
|---|-------|------|------------|--------|
| [01 — SSOT wire-up: `bdg_pfaffian_params_t` from default to call site](issues/01-ssot-wire-up.md) | task (AFK) | — | **closed** (2026-07-14) |
| [02 — Orphan retirement: delete dense `wire_pfaffian_witness`](issues/02-orphan-retirement.md) | task (AFK) | 01 ✓ | **closed** (2026-07-13) + **executed** (2026-07-25; 51/51 unit PASS) |
| [03 — Test contracts: SSOT error-stops + s2_sign tightening + meta-test](issues/03-test-contracts.md) | task (AFK) | 01 ✓ | **closed** (2026-07-13) + **executed** (2026-07-25; 51/51 unit PASS; SD-1 dropped infeasible — pFUnit 4.16 can't catch `error stop`; SD-2 already done by ticket 01; SD-3/4/5/6/7 landed) |
| [04 — Codacy triage: 2 high `ErrorProne` flags](issues/04-codacy-triage.md) | task (HITL/AFK) | — | **closed** (2026-07-13) + **executed** (2026-07-25; 2x drop-f-prefix applied) |
| [05 — Verification gate: unit-count + ctest + clean build](issues/05-verification-gate.md) | task (AFK) | 01 ✓, 02 ✓, 03 | **closed** (2026-07-25) + **executed** (2026-07-25; 51/51 unit PASS; 3-witness gate green; unit-count.txt locked) |
| [06 — Spec amendment: User Story 5 verbatim text](issues/06-spec-amendment.md) | task (AFK) | 03 | **closed** (2026-07-25) + **executed** (2026-07-25; verbatim design.md §"Spec Amendment" text + meta-test citation landed in `spec.md:95`) |
| [07 — Doc propagation: HANDOFF files + 5 doc sites](issues/07-doc-propagation.md) | task (AFK) | 01 ✓, 03, 05, 06 | **closed** (2026-07-25) + **executed** (2026-07-25; re-charted then executed — premise correction: 51 not 52, 3-witness not 4-witness live, PR #42 open not shipped; 5 doc sites carry User Story 5 verbatim; 2 HANDOFF files created as pointer docs) |
| [08 — Memory entry: project_bdg_evaluator_seam_ssot follow-on](issues/08-memory-entry.md) | task (AFK) | 06, 07 | **closed** (2026-07-25) + **executed** (2026-07-25; updated existing memory in place — S2-only/3-witness/`bdg_pfaffian_params_t`/User Story 5/51 count; MEMORY.md hook refreshed) |

Frontier (open, unblocked, unclaimed, lowest number first): **empty** — all eight tickets executed (01✓, 02✓, 03✓, 04✓, 05✓, 06✓executed-25jul, 07✓executed-25jul, 08✓executed-25jul). Remaining step is PR #42 close-out (review sign-off + umbrella-merge of `feat/bdg-u2-actual-ship` — not a ticket; tracked on the PR timeline), then this map auto-archives to `.scratch/archive/bdg-u2-finish/`.
